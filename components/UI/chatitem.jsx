import React, { useState, useEffect, useCallback } from "react";
import firebase from "firebase";
const ChatItem = (props) => {
  const { item, msgId, uid } = props;
  const [info, setInfo] = useState();
  return <div></div>;
};
export default ChatItem;
